# 09B Task 07 — GitHub 构建、部署与最终 QA

## 实施提示词

```text
仅执行新版 PLAN/09B-production-content-and-cloudbase.md Task 7。禁止子代理、委派、amend 和 push。先实现并本地验证 verify:release 与只读 CI；GitHub 绑定和 CloudBase 首次部署必须分别获得用户明确授权，缺任一授权就停在对应门前。

获权后将 main 配置为 npm ci、npm run verify:release、dist、路径 /，不引入数据库、函数、容器或 EdgeOne。部署后记录 SHA/HTTPS/回滚，并在四视口及三浏览器完成八馆旅程、三维地图、红心、飞行、内容、媒体、字幕、弱网、错误恢复、键盘、reduced-motion 和溢出检查。所有自动命令及人工必需项通过后，只建 Task 7 独立本地提交；验收通过不等于自动获得最终公开发布授权。
```

## 验收提示词

```text
最终验收新版 09B Task 7。禁止子代理、修改、重新部署或发布。独立核实 GitHub 绑定与部署的两项授权、CI 权限、部署 SHA、实际 HTTPS、四视口三浏览器和八馆完整旅程；复跑 verify:release。确认三维地图技术检查通过且页面没有声称衍生三维地图取得新审图批准。全部有证据才写“09B 验收通过，可向用户申请最终公开发布授权”；否则生成阻断修复提示词。
```

## 通过后

停止自动执行，向用户单独申请最终公开发布授权，并提醒 CloudBase 体验版于 `2027-02-21 23:59:59` 到期。

## 未通过时的修复提示词

```text
修复新版 09B Task 7，禁止子代理，不 amend、不 push、不自动重新部署。验收问题：
【粘贴验收问题】
先分类本地、内容、媒体、地图技术或云配置问题；任何重新绑定、重新部署、权限修改或对象写入必须先取得新授权。修复后重新执行完整验收。
```
