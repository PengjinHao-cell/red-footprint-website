# 2026-08-23 基线记录:地球与媒体现状

> 日期:2026-08-23
> 阶段:维护 Task 0(基线记录),本阶段不改动业务代码
> 项目目录:`/Users/pengjinhao/Documents/AiPlace/暑期红色网站`
> 依据:暑期红色网站问题修复方案与实施步骤

## 1. Git 与工作区状态(Step 1)

| 项目 | 记录 |
| --- | --- |
| 项目目录内 `.git` | 不存在;`git rev-parse --show-toplevel` 解析到父目录 `/Users/pengjinhao/Documents/AiPlace` |
| 父仓库 `git status --short` | 217 行:147 行 `D`、69 行 `??`、1 行 `M`(quiz-app);其中 `暑期红色网站/` 整体显示为未跟踪 `??` |
| 处理 | 按计划不清理、不恢复、不暂存任何现有改动;基线仅新增 `docs/maintenance/` 下文档 |

注:父仓库历史中将 `暑期红色网站/` 记录为 gitlink(submodule 指针),而磁盘上是完整项目目录,因此父仓库状态呈现上述形态;本项目实际变更管理以本次维护产生的本地提交为准,不在本阶段处理父仓库历史。

## 2. 现有验证结果(Step 2)

| 验证 | 退出码 | 结果 |
| --- | --- | --- |
| `npm run test:run` | 0 | 25 个测试文件、185 个测试全部通过(vitest 4.1.11) |
| `npm run lint` | 0 | eslint,0 错误 0 警告 |
| `npm run build` | 0 | `tsc -b` + `vite build` 成功 |

构建产物(生产构建,2026-08-23 13:34 左右生成):

| 资源 | 体积 | gzip |
| --- | --- | --- |
| `dist/index.html` | 0.41 kB | 0.31 kB |
| `dist/assets/index-*.css` | 9.87 kB | 2.74 kB |
| `dist/assets/index-*.js`(主代码块) | 1,065.93 kB | 338.62 kB |
| `dist/assets/globe.gl-*.js`(地球代码块,动态导入) | 1,883.65 kB | 532.59 kB |

## 3. 资源体积(Step 3)

| 资源 | 字节 | 说明 |
| --- | --- | --- |
| `src/data/china-globe-map.json` | 578,090 | 静态引用进主代码 |
| `src/data/sites.json` | 154,732 | 生成后的站点数据 |

## 4. 三档视口测量(Step 4)

测量方法:生产构建经 `vite preview` 提供,Playwright headless Chromium(软件渲染 SwiftShader),`deviceScaleFactor: 1`;每档运行 3 次,取中位数;时间以 `Date.now()` 差值计。本机为受限执行沙箱,数值代表同一环境的相对基线,不直接等同于真实 GPU 设备体验。

中位数结果(三次测量,ms):

| 视口 | 载入→按钮可用 | 点击→首次出现 canvas | 点击→8 颗星就绪且位置稳定 | 屏幕上可辨认星簇数 |
| --- | ---: | ---: | ---: | ---: |
| 390×844(mobile) | 63 | 11,938 | 16,950 | 1 |
| 768×1024(tablet) | 37 | 17,813 | 31,030 | 1 |
| 1440×900(desktop) | 75 | 19,194 | 35,682 | 1 |

每次测量均记录到 8 个 `button[aria-label=站点名]` 全部存在于 DOM(8/8),但 8 个按钮的屏幕坐标全部重叠在约 30×30 CSS px 区域内(例如桌面档 x≈860–900、y≈492–517),故可辨认星簇数为 1:远视图下同城站点(上海 2、南京 3、扬州 2、宿迁 1)视觉上无法区分,且 `pointsData` 三维柱点与 HTML 星同时存在(圆柱体根因)。

截图:已生成 `baseline-{mobile,tablet,desktop}-{1,2,3}.png` 供人工复核(测量脚本临时产物,不提交)。

## 5. 媒体现状

`content/media/media-manifest.json` 中每站点照片数:

| 站点 | 照片数 |
| --- | ---: |
| sihong-memorial 淮北抗日民主根据地纪念馆 | 2 |
| yuhuatai-martyrs 雨花台烈士陵园 | 5 |
| dujiang-victory 渡江胜利纪念馆 | 5 |
| sihang-warehouse 上海四行仓库抗战纪念馆 | 5 |
| cpc-first-congress 一大会址纪念馆 | 5 |
| jiangshangqing-memorial 江上青烈士史料陈列馆 | 1 |
| yangzhou-martyrs 扬州革命烈士陵园 | 1 |
| meiyuan-new-village 梅园新村纪念馆 | 4 |
| **合计** | **28** |

每站点另有 1 video、1 poster、1 captions(VTT)。

## 6. 已确认的代码事实(基线快照)

- `src/components/globe/GlobeScene.tsx` 同时调用 `htmlElementsData`(字符 ★)与 `pointsData`(三维柱状点)+ `pointRadius`/`pointAltitude`,并调用 `ringsData` 为未访问站点绘制扩散光环。
- 初始视角 `getChinaOverview(width)`:手机 `{lat:35, lng:104, altitude:2.05}`,平板 `{lat:35, lng:104, altitude:1.45}`,桌面 `{lat:35, lng:104, altitude:1.25}`(全国视角)。
- `MAX_DEVICE_PIXEL_RATIO = 1.5`;渲染器 `antialias: true`;地图数据静态 import。
- 欢迎页 `WelcomeScreen` 由 `App.tsx` 直接以 `ready` 传入,按钮不等待地球;点击后才挂载 `GlobeScene` 并动态 `import('globe.gl')`。
- 本阶段未修改任何业务代码、测试或媒体文件。

## 7. 后续对比基准

优化后将以同一机器、同一构建、同一网络配置、同一测量脚本复测,并给出前后中位数对比。

## 8. 第一轮优化结果(Task 4)

第一轮:设备渲染预算(桌面像素比上限 1.25、移动端关闭抗锯齿与 rings)+ 关闭默认入场动画 `animateIn: false`。测量对比(中位数,ms):

| 视口 | 基线 click→ready | 第一轮 click→ready | 改善 |
| --- | ---: | ---: | ---: |
| 390×844 | 16,950 | 14,294 | -16% |
| 768×1024 | 31,030 | 21,732 | -30% |
| 1440×900 | 35,682 | 23,037 | -35% |

仍未达到目标(桌面 3 秒 / 移动 5 秒),进入第二轮:离线地图简化。

## 9. 第二轮优化结果(Task 4)

第二轮:将 `src/data/china-globe-map.json`(578,090 字节 / 25,240 坐标点)离线 Douglas-Peucker 简化,生成 `src/data/china-globe-map.simplified.json`(184,648 字节 / 7,858 坐标点,-69% 体积),34 省 + 1 南海边界保留,最短环(澳门)保留 9 点。`mapSource.json` 的 `runtimeResource` 更新为简化文件与 SHA-256;`npm run check:map` 全部通过。主代码块:1,065.93 kB → 677.53 kB(-36%)。

第二轮测量(中位数,ms):

| 视口 | 基线 | 第一轮 | 第二轮 click→ready | 总改善 |
| --- | ---: | ---: | ---: | ---: |
| 390×844 | 16,950 | 14,294 | 10,550 | -38% |
| 768×1024 | 31,030 | 21,732 | 15,688 | -49% |
| 1440×900 | 35,682 | 23,037 | 17,861 | -50% |

## 10. 路线 A 两轮优化停止结论

- 两轮独立假设与测量已完成,按方案第 8 节"路线 A 最多进行两轮",不再追加第三轮补丁。
- 目标(桌面 3 秒 / 移动 5 秒)在本测量环境(Playwright headless Chromium + SwiftShader 软件渲染)下未达成;软件渲染是主要瓶颈,不代表真实 GPU 设备表现,需在真机/GPU 环境复测后判断是否触发路线 B(直接 Three.js 本地对照版)。
- 已改善项:构建主块 -36%、两轮 ready 中位数 -38%~-50%、地图资源 -69%;8 星、镜头、降级与重试行为未回归。
- 结论状态:`REVIEW_ROUTE_B`(性能未在本环境达标,等待项目方批准制作路线 B 本地对照版)或真机复测达标后改为 `ACCEPT_ROUTE_A`,二者由项目方决定。
