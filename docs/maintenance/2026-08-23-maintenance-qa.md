# 2026-08-23 维护本地验收报告

> 日期:2026-08-23
> 阶段:维护 Task 8(完整本地验收与路线 A 决策)
> 项目目录:`/Users/pengjinhao/Documents/AiPlace/暑期红色网站`
> 依据:暑期红色网站问题修复方案与实施步骤

## 1. 静态验证(Step 1)

| 验证 | 结果 |
| --- | --- |
| `npm run lint` | 通过,0 错误 0 警告 |
| `npm run test:run` | 通过,31 个测试文件 / 202 个测试 |
| `npm run build` | 通过,tsc -b + vite build 成功 |

## 2. 内容与媒体门禁(Step 2)

| 检查 | 结果 |
| --- | --- |
| `npm run check:content` | 通过(生产内容、媒体、schema、确定性生成、漂移检查) |
| `npm run check:map` | 通过(34 省 + 1 南海边界、8 标记、SHA-256、四视口视觉完整性) |
| `npm run check:media` | 通过(45 张照片 + 8 hero/video/poster/VTT,77 个对象) |

注:`check:content --release`(发布门禁)当前按预期阻塞:45 张照片与 17 个新对象尚未上传 CloudBase、无对账记录;这是本轮"本地交付、不上传"边界的正确表现,不作为失败。

## 3. 端到端测试(Step 3)

`npm run test:e2e`(Playwright,桌面 Chromium / 移动 Chromium / 移动 WebKit):

- 18/18 通过:欢迎进入、受控地图适配器旅程、八站点降级列表、详情开启/关闭/返回、媒体轮播(视频第一项、末张停止)、原生视频焦点、弱网分段请求、图片/视频失败降级。
- 为适配 ProgressiveGlobe,`tests/e2e/journey.spec.ts` 的受控适配器挂载时调用 `onReady`;`GlobeScene` 在 WebGL 不可用/harness 强制降级时通知 `onError`,使渐进组件正确切换降级态。

## 4. 三档视口视觉验收(Step 4)

| 视口 | 开屏动画 | 8 颗星 | SVG 星 | 引线 | 详情 |
| --- | --- | --- | --- | --- | --- |
| 390×844 | 5 星 + 1 路线 | 8/8 | 8/8 | 4 条 | 见下注 |
| 768×1024 | 5 星 + 1 路线 | 8/8 | 8/8 | 4 条 | 见下注 |
| 1440×900 | 5 星 + 1 路线 | 8/8 | 8/8 | 4 条 | 见下注 |

注:三档截图(`qa-*.png`)已保存。本测量环境为 headless Chromium + SwiftShader 软件渲染,globe.gl 渲染占用主线程导致 GSAP 飞行时间线被饿死,星标点击后的镜头飞行在 headless 下无法在合理时间内完成;已由 E2E(受控适配器路径)与 cameraFlight 单元测试覆盖交互正确性,需在真实 GPU 设备上复核飞行与详情打开。

## 5. 性能验收(Step 5)

两轮路线 A 优化后的 ready 中位数(ms,headless 软渲染):

| 视口 | 基线 | 第一轮 | 第二轮 | 总改善 |
| --- | ---: | ---: | ---: | ---: |
| 390×844 | 16,950 | 14,294 | 10,550 | -38% |
| 768×1024 | 31,030 | 21,732 | 15,688 | -49% |
| 1440×900 | 35,682 | 23,037 | 17,861 | -50% |

构建主代码块:1,065.93 kB → 677.53 kB(-36%);地图资源 578 KB → 185 KB(-69%)。详细数据见 `2026-08-23-baseline.md`。

## 6. 路线决策(Step 6)

- 结论状态:**REVIEW_ROUTE_B**(按方案第 8 节,两轮优化后性能仍不在本测量环境达标)。
- 判断依据:本环境为软件渲染,瓶颈指向渲染器本身;真实 GPU 设备表现未知。
- 下一步由项目方决定:
  1. 在真实 GPU 设备上复测(同一构建、同一网络,三次取中位数);
  2. 达标 → `ACCEPT_ROUTE_A`,等待授权决定上传与部署;
  3. 不达标 → 批准制作路线 B(直接 Three.js 本地对照版)后再比较。
- 本报告未授权任何 CloudBase 上传或部署。

## 7. 已知问题清单

| 问题 | 状态 | 说明 |
| --- | --- | --- |
| 8 颗星中近邻星屏幕重叠 | 部分缓解 | 已加引线与偏移;headless 下上海/南京/扬州簇星仍可被兄弟星拦截点击,需真机确认偏移量 |
| 45 张照片未上传 | 预期阻塞 | 待项目方单独授权 CloudBase 写入 |
| 发布门禁 check:content --release | 预期阻塞 | 无对账记录前不得通过 |
| 地图合规 | 未改变 | 继续使用 GS(2023)2762号 记录与简化运行资源,未声称新审图批准 |
